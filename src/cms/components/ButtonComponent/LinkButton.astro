---
export interface Props {
    label?: string;
    link?: string | null;
    cssClassses?: string | string[];
}
const {link="/", cssClassses } = Astro.props as Props;

---

<a class="btn py-2 px-4" class:list={cssClassses} href={link}>
    <slot />
</a>
