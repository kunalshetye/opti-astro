fragment GenericProductList on GenericProductList {
    UseProductCatalog
    PreSelectedCategory
    ListedProducts {
        _metadata {
            types
            key
        }
        ...GenericProduct
    }
}
